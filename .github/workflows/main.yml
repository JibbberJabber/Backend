


on: [push]

name: AzureDeploy

jobs:

    deploy:
      runs-on: ubuntu-latest
      steps:

      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
          
      - name: look for ps1 file
        run: |
            ls '${{ env.OUTPUT_PATH }}\vmScript.ps1'
